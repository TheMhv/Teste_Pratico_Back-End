// Preparando ambiente para o projeto
    composer create-project laravel/laravel API_OnFly
    cd API_OnFly

// Criando e configurando o banco de dados
    php artisan make:migration criar_tabela_despesas --create=despesas
    php artisan make:migration criar_tabela_usuarios --create=usuarios
    // Editar os arquivos de migração
    php artisan migrate

    php artisan make:seeder UsuariosSeeder
    // Editar os arquivos de seeder
    php db:seed --class=UsuariosSeeder

// Criar e configurar os arquivos de Models
    php artisan make:model DespesasModel
    php artisan make:model UsuariosModel
    // Editar os arquivos de models

// Criar e configurar os arquivos de Resource
    php artisan make:resource DespesasResource
    php artisan make:resource UsuariosResource
    // Editar os arquivos de resources

// Criar e configurar os arquivos de Controller
    php artisan make:controller DespesasController --resource
    php artisan make:controller UsuariosController --resource
    // Editar os arquivos de controllers

// Configurar e apontar as rotas para os controllers
    // Editar o arquivos de rota de api